{
  "name": "terraria-player-parser",
  "version": "1.0.0",
  "description": "",
  "main": "terraria-player-parser.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test:node": "node ./src/tests/node.js",
    "server": "parcel serve --port 80 --open --out-dir src/tests --out-file browser-built.html --target browser --cache-dir src/tests/.cache src/tests/browser.html",
    "build:browser": "parcel build --out-dir ./ --out-file terraria-player-parser-browser.min.js --no-cache --target browser --no-source-maps src/browser/terraria-player-parser.js"
  },
  "keywords": [],
  "author": "cokolele",
  "license": "ISC",
  "dependencies": { //dont forget to install this when using browser version
    "aes-js": "^3.1.2"
  },
  "devDependencies": { //just for testing
    "@babel/polyfill": "^7.2.5",
    "parcel-bundler": "^1.11.0"
  }
}
